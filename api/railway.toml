[build]
builder = "dockerfile"
dockerfilePath = "api/Dockerfile"

# Watch only API directory
watchPatterns = ["api/**"]

[deploy]
startCommand = "node dist/server.js"
healthcheckPath = "/v1/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
